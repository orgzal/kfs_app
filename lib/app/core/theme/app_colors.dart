import 'package:flutter/material.dart';

abstract class AppColors {
  // Official Governorate Colors
  static const primary = Color(0xFF132A46); // Dark Navy / Charcoal
  // static const primary = Color(0xFF132A46); // Dark Navy / Charcoal
  static const secondary = Color(0xFFD8A303); // Warm Gold / Mustard Gold
  // static const secondary = Color(0xFFCBA258); // Warm Gold / Mustard Gold
  static const accent = Color(0xFFE5B962);
  static const background = Color(0xFFF7F8FA); // Very Light Gray sections
  static const surface = Color(0xFFFFFFFF); // White
  static const border = Color(0xFFE9EDF2);
  static const textPrimary = Color(0xFF1B2430);
  static const textSecondary = Color(0xFF6C7A8A);
  static const error = Color(0xFFD32F2F);
  static const Color colorWhite = Color(0xFFFFFFFF);
  static const Color backgroundDark = Color(0xFF121212);
  static Color grey = Color(0XFFf1f3f6);
}
